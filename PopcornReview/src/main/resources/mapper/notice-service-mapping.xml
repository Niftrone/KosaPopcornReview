<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ns.sql.NoticeMapper">

<insert id="addNotice" parameterType="notice"
        useGeneratedKeys="true" keyProperty="noticeId">
    
    INSERT INTO notice (notice, notice_plot, notice_date)
    VALUES (#{notice}, #{noticePlot}, NOW())

</insert>
	<delete id="deleteNotice" parameterType="int">
		DELETE FROM notice
		WHERE notice_id = #{noticeId}
	</delete>
	
	<update id="updateNotice" parameterType="notice">
		UPDATE notice
		SET notice = #{notice}, notice_plot = #{noticePlot}
		WHERE notice_id = #{noticeId}
	</update>
	
	<!-- 동적쿼리 : 공지사항 list + 공지사항 자세히 보기 -->
<select id="getNotices" parameterType="notice" resultType="notice">
    SELECT 
        notice_id, 
        notice, 
        notice_plot, 
        notice_date
    FROM 
        notice
    <where>
        <if test="noticeId != null and noticeId != 0">
            AND notice_id = #{noticeId}
        </if>
        <if test="notice != null and notice != ''">
            AND notice LIKE CONCAT('%', #{notice}, '%')
        </if>
    </where>
</select>
</mapper>

 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
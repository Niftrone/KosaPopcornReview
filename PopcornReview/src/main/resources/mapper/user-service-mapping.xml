<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ns.sql.UserMapper">

	<insert id="addUser" parameterType="user">
		INSERT INTO user (id, pwd, email, name, birthdate, phone, gender)
		VALUES(#{id}, #{pwd}, #{email}, #{name}, #{birthdate}, #{phone}, #{gender})
	</insert>
	
	<update id="updateUser" parameterType="user">
		UPDATE user 
		SET pwd = #{pwd}, email = #{email}, name = #{name}, phone = #{phone}
		WHERE id = #{id}    
	</update>
	
	<delete id="deleteUser" parameterType="string">
		DELETE FROM user
		WHERE id = #{id}
	</delete>

	<select id="getUser" parameterType="user" resultType="user">
		SELECT id, pwd, email, name, birthdate, phone, gender
		FROM user
		WHERE 1=1
		<choose>
			<when test="name != null and phone != null">
				AND name = #{name}
				AND phone = #{phone}
			</when>
			<when test="id != null and email != null">
				AND id = #{id}
				AND email = #{email}
			</when>
			<otherwise>
				AND id = #{id}
				AND pwd = #{pwd}
			</otherwise>
		</choose>
	</select>
</mapper>

 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 